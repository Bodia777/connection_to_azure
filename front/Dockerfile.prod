# Dockerfile.prod
FROM node:22-alpine AS build

WORKDIR /app
COPY . .

RUN corepack enable && corepack prepare pnpm@10 --activate
RUN pnpm install
RUN pnpm run build:prod

FROM nginx:alpine
COPY --from=build /app/dist/docker-example /usr/share/nginx/html

EXPOSE 80
